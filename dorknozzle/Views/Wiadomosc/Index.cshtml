@model IEnumerable<dorknozzle.Models.Wiadomosc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutDorknozzle.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Subject)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShortContent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Content)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Subject)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShortContent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Content)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WiadomoscId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WiadomoscId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WiadomoscId })
        </td>
    </tr>
}

</table>
